void increment(){
    while(onBeeper()){
        if(onBeeper()){
            pickBeeper();
            moveForward();
        }
        else{
            dropBeeper();
        }
    }
    dropBeeper();
}
